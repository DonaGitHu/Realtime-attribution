Day 1 — GA4 BigQuery Exploration
Queries
1. Preview rows
SELECT * 
FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_20201101`
LIMIT 100;
2. Total events count
SELECT COUNT(*) AS total_events
FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_20201101`;
3. Page titles from event_params
SELECT 
  ep.value.string_value AS page_title, 
  COUNT(*) AS views
FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_20201101`,
UNNEST(event_params) ep
WHERE ep.key = 'page_title'
GROUP BY page_title;
Notes
Each row = one GA4 event.
event_params is a repeated field → must UNNEST.
